import React from 'react';
import { Card } from 'antd';

const DebugAssistant: React.FC = () => {
    return (
        <Card title="AI Debug Assistant">
            <p>Interactive debug session will be here.</p>
        </Card>
    );
};

export default DebugAssistant;
