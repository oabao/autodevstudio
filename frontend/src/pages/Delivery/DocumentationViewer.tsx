import React from 'react';
import { Card } from 'antd';

const DocumentationViewer: React.FC = () => {
    return (
        <Card title="Documentation Viewer">
            <p>Documentation content would be displayed here.</p>
        </Card>
    );
};

export default DocumentationViewer;
