pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                // Add build steps here, e.g., mvn clean install, npm install
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                // Add test steps here, e.g., mvn test, npm test
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                // Add deployment steps here, e.g., kubectl apply -f ...
            }
        }
    }
}
